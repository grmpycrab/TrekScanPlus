# Flutter app no longer uses a .env file.
# All email sending is handled server-side via Firebase Cloud Functions.
#
# To configure email for Cloud Functions, set the SendGrid key in:
#   functions/.env
#
#   SENDGRID_KEY=SG.your-sendgrid-api-key
